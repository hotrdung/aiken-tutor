{
  "name": "gift-card",
  "version": "1.0.0",
  "description": "Write validators in the `validators` folder, and supporting functions in the `lib` folder using `.ak` as a file extension.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@meshsdk/core": "^1.9.0-beta.61",
    "@meshsdk/core-csl": "^1.9.0-beta.62",
    "dotenv": "^17.2.0",
    "tsx": "^4.20.3"
  }
}
